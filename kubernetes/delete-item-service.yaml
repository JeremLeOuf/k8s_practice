apiVersion: v1
kind: Service
metadata:
  name: delete-item-service
  labels:
    app: delete-item
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: delete-item

